[gd_scene load_steps=6 format=2]

[ext_resource path="res://tank.svg" type="Texture" id=1]
[ext_resource path="res://gum_base.svg" type="Texture" id=2]
[ext_resource path="res://gun.svg" type="Texture" id=3]

[sub_resource type="GDScript" id=3]
script/source = "extends KinematicBody2D

var speed = 400
var velocity = Vector2.ZERO
onready var right_joystick = $\"../right_joystick\"
onready var left_joystick = $\"../left_joystick\"
onready var body = $body
onready var gun = $gun
onready var body_collision = $body_collision

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _physics_process(delta):
	var vector = left_joystick.output
	velocity = vector * speed
	var angel_diff = Vector2.RIGHT.rotated(self.rotation).angle_to(vector)
	
	if abs(angel_diff) < 0.3:
		#var acceleration = 1 - (angel_diff/0.2)
		move_and_slide(velocity)
	
	if velocity != Vector2.ZERO:
		self.rotation = lerp_angle(rotation, vector.angle(), 0.15)
	
	if right_joystick.output != Vector2.ZERO:
		gun.rotation = lerp_angle(gun.rotation, right_joystick.output.angle() - self.rotation,0.3)
	else: gun.rotation = lerp_angle(gun.rotation, 0, 0.1)
	
	body_collision.rotation = body.rotation
	#gun_collision.rotation = gun.rotation
"

[sub_resource type="RectangleShape2D" id=1]

[node name="tank" type="KinematicBody2D"]
position = Vector2( 679.838, 306.36 )
scale = Vector2( 0.250468, 0.250468 )
script = SubResource( 3 )

[node name="body" type="Sprite" parent="."]
self_modulate = Color( 0.180392, 0.356863, 1, 1 )
texture = ExtResource( 1 )

[node name="body_collision" type="CollisionShape2D" parent="."]
scale = Vector2( 15.8876, 12.9075 )
shape = SubResource( 1 )

[node name="gun" type="Sprite" parent="."]
self_modulate = Color( 0, 0.0509804, 0.278431, 1 )
position = Vector2( -20, 0 )
texture = ExtResource( 2 )

[node name="Sprite" type="Sprite" parent="gun"]
self_modulate = Color( 0, 0.00392157, 0.572549, 1 )
texture = ExtResource( 3 )
offset = Vector2( 90, 0 )
